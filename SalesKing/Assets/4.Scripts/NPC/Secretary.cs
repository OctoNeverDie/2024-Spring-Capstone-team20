using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Secretary : MonoBehaviour
{
    /*
    public GameObject Neck;
    public GameObject Panel;

    void Update()
    {
        Neck.transform.LookAt(Managers.Player.MyPlayer.transform.position);
    }

    public void ShowPanel()
    {
        Panel.SetActive(true);
        StartCoroutine(HidePanelAfterDelay(3f)); // 3초 후에 패널 숨김
    }

    private IEnumerator HidePanelAfterDelay(float delay)
    {
        yield return new WaitForSeconds(delay); // delay만큼 대기
        Panel.SetActive(false); // 패널 비활성화
    }
    */
}
